apiVersion: v1
kind: Service
metadata:
  name: conexao-solidaria-api-service
  namespace: conexao-solidaria
spec:
  selector:
    app: conexao-solidaria-api

  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080

  type: NodePort